<Page x:Class="Hangman.Views.UserSettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Hangman.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="UserSettingsPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="350" />

        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" HorizontalAlignment="Center">Ändra din användare</Label>
        <Label Grid.Row="0" Grid.Column="1" HorizontalAlignment="Center">Ta bort din användare</Label>

        <Grid Grid.Row="1" Grid.Column="0">
            <Frame x:Name="ChangeUserFrame" Grid.Row="1"></Frame>
        </Grid>

        <Grid Grid.Row="1" Grid.Column="1">
            <Frame x:Name="DeleteUserFrame" Grid.Row="1"></Frame>
        </Grid>
    </Grid>
</Page>
